<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
      <%# ログインしているときは以下を表示する %>
        <div class="greeting"> 
          <%=link_to "こんにちは、#{current_user.name}さん" ,"/users/#{current_user.id}" , class: :greeting_link%>
          <%= render partial: "prototype", collection: @prototypes %>
        </div>
    <% end %>
      <div class="card__wrapper">
         <%= render partial: "prototype", collection: @prototypes %>
      </div>
</main>
